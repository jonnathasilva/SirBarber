export const HeaderMain = ({ title, topics }) => (
  <div>
    <span className="uppercase text-red-300 text-md font-bold">{topics}</span>

    <h2 className="text-4xl font-bold leading-5xl text-Black-500">{title}</h2>
  </div>
);
